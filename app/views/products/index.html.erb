<!DOCTYPE html>
<html>
<head>
  <title>Shopping cart</title>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <% if request.ssl? %>
    <%= stylesheet_link_tag 'https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css' %>
    <%= javascript_include_tag 'https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js' %>
  <% else %> 
    <%= stylesheet_link_tag 'http://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css' %>
    <%= javascript_include_tag 'http://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js' %>
  <% end %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <div class="row">
      <div class="col-xs-3 col-sm-3 col-lg-3">
        <%= render "filter"%>
      </div>
      <div class="col-xs-9 col-sm-9 col-lg-9">
        <div class="page-header"><h1>Products</h1></div>
        <div class="ezhil" id="test">
          <%= render @products %>
        </div>
      </div>
    </div>
    <hr>
  </div>

</body>
</html>
<script>
$( document ).ready(function() {
  $("#product_filter").change(function() {
   var substitute = $("#product_filter :selected").text();
    alert($("#product_filter :selected").text());
   $.ajax({
    type: "POST",
    url: "products/filter_index",
    data: {name: substitute},
    success: function(data) {
      // Handle stuff after hitting the server here
    },
    error: function(data) {
    }
  });
   //  $.ajax({
   //    alert("karthick");
   //   type: "POST",
   //   url: '/products/filter_index',
   //   data: {  name : $("#product_filter").val()}
   // });
   // // AJAX call for updating the round robin status for the BD agent
 });
});
</script>






