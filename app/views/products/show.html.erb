<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Price:</strong>
  <%= @product.price %>
</p>

<p>
<strong>Active:</strong>
<%= @product.active %>
</p>


<%= link_to image_tag(@product.image.url(:original), class: 'media-object'), @product.image.url, target: '_blank' %>
<%= link_to 'Back', products_path, :class => 'btn btn-success'  %>
<%= link_to 'Edit', edit_product_path(@product), :class => 'btn btn-warning' %>
<%#= link_to t('.destroy', :default => t("helpers.links.destroy")),
              product_path(@product),
              :method => 'delete',
              :data => { :confirm => ('confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
<%= link_to 'Add to cart',
              products_path, :class => 'btn btn-warning'  %>











